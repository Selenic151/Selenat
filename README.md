# Selenat
An small realtime chat app

To install this app

Step 1: Create node project
npm init -y

Step 2: Install node API Express
 npm install express
 
Step 3: Install nodemon to auto refresh after have change in your code
npm install --save-dev nodemon

Step 4: Connect to MongoDB
npm install mongoose

Step 5: Create env variable
npm install dotenv

